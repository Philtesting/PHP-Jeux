{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
<h1>Résumé compte</h1>
<p>Bonjour {{ user.username }}</p>

<p>Score Personnel</p>
<table class="table table-stripped">
    <thead>
        <tr>
            <td>Jeux</td>
            <td>Difficulté</td>
            <td>Score</td>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Quizz</td>
            <td>Facile</td>
            <td>{{user.scoreFacil}}</td>
        </tr>
    </tbody>
    <tbody>
        <tr>
            <td>Quizz</td>
            <td>Moyen</td>
            <td>{{user.scoreMoyen}}</td>
        </tr>
    </tbody>
    <tbody>
        <tr>
            <td>Quizz</td>
            <td>Difficile</td>
            <td>{{user.scoreDifficil}}</td>
        </tr>
    </tbody>
</table>
<br>
<a href="{{ path('security_account_username') }}" class="btn btn-success">Modifier son pseudo</a></br></br>
<a href="{{ path('security_account_pass') }}" class="btn btn-success">Modifier son mot de passe</a>
{% endblock %}
